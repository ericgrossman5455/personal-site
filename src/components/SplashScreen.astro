<div class='splash-screen'>
	<lottie-player src='/assets/logo_anim.json' background='transparent' speed='1' autoplay
	></lottie-player>
</div>

<style lang='scss'>
	.splash-screen {
		position: fixed;
		z-index: 999;
		background-color: var(--bg-layer-base);
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		width: 100%;
		height: 100%;
		display: flex;
		align-items: center;
		justify-content: center;
		animation: logo-anim 1s ease-in 1.8s 1 forwards;

		lottie-player {
			width: 20%;
			height: 20%;
			max-width: 200px;
			max-height: 200px;
			min-width: 125px;
			min-height: 125px;
			filter: invert(0.2);

			@media (prefers-color-scheme: dark) {
				filter: invert(0.9);
			}
		}

		@keyframes logo-anim {
			99% {
				transform: scale(200);
				opacity: 0;
			}
			100% {
				transform: scale(200);
				opacity: 0;
				pointer-events: none;
			}
		}
	}
</style>
